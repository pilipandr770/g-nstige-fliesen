# Синхронизация контента производителей

## Обзор системы

Система автоматически извлекает контент (коллекции, проекты, статьи) с официальных сайтов производителей плитки и публикует его на нашей платформе.

## Архитектура

### 1. Индивидуальные парсеры (app/services/manufacturer_parsers.py)

Для каждого производителя создан специализированный парсер, который знает структуру HTML его сайта:

- **ApariciParser** - для Aparici.com
- **DuneParser** - для DuneCeramics.com
- **EquipeParser** - для EquipeCeramicas.com
- И другие...

### 2. Система фильтрации

**Важно**: Система показывает только коллекции и проекты с изображениями!

#### Валидация при синхронизации:
- ✅ **Сохраняются**: элементы с названием И изображением
- ⚠️ **Пропускаются**: элементы без изображения или без названия
- 📊 **Статистика**: после синхронизации показывается сколько добавлено и сколько пропущено

#### Фильтрация при отображении:
```python
# В routes.py - показываем только с изображениями
collections = ManufacturerContent.query.filter_by(
    content_type='collection',
    published=True
).filter(
    ManufacturerContent.image_url.isnot(None), 
    ManufacturerContent.image_url != ''
).all()
```

## Как работает синхронизация

### 1. Извлечение контента

```python
# Система автоматически выбирает парсер
custom_parser = ManufacturerParserFactory.get_parser(manufacturer_slug)

if custom_parser:
    # Используется индивидуальный парсер
    collections = custom_parser.extract_collections()
else:
    # Используется базовый универсальный парсер
    collections = base_parser.extract_collections()
```

### 2. Скачивание изображений

Изображения скачиваются локально с механизмом retry (3 попытки):

```python
def download_image(image_url, retry_count=3):
    for attempt in range(retry_count):
        try:
            # Скачать и сохранить
            # Проверить размер файла
            # Вернуть локальный путь
        except Timeout:
            # Повторить попытку
        except RequestException:
            # Повторить попытку
```

**Сохраняются в**: `app/static/uploads/manufacturers/`
**Формат имени**: `{slug}_{hash}.jpg`

### 3. Валидация перед сохранением

```python
for item in collections:
    # Проверка изображения
    if not item.get('image_url'):
        print("⚠️  Пропущена коллекция без изображения")
        count_skipped += 1
        continue
    
    # Проверка названия
    if not item.get('title') or len(item.get('title', '')) < 2:
        print("⚠️  Пропущена коллекция без названия")
        count_skipped += 1
        continue
    
    # Сохранить в БД
    db.session.add(ManufacturerContent(...))
    count_added += 1
```

## Добавление нового производителя

### Шаг 1: Анализ сайта

Используйте `analyze_aparici_html.py` как шаблон:

```python
python analyze_aparici_html.py
```

Это покажет:
- Структуру HTML
- CSS классы
- Ссылки на коллекции
- Расположение изображений

### Шаг 2: Создание парсера

В `app/services/manufacturer_parsers.py`:

```python
class MyManufacturerParser(BaseManufacturerParser):
    def __init__(self):
        super().__init__('https://example.com/de', 'my-manufacturer')
    
    def extract_collections(self) -> List[Dict]:
        soup = self.fetch_page(f"{self.base_url}/collections")
        
        # Поиск ссылок на основе реальной HTML структуры
        collection_links = soup.find_all('a', class_='specific-class')
        
        for link in collection_links[:20]:
            # Извлечь название, URL
            # Получить детали страницы
            # Скачать изображение
            # Добавить в список
            
        return collections
    
    def extract_collection_detail(self, url: str) -> Dict:
        soup = self.fetch_page(url)
        
        # Найти изображение
        # Найти описание
        # Найти технические характеристики
        
        return {
            'image_url': main_image,
            'description': description,
            'full_content': content,
            'technical_specs': specs
        }
```

### Шаг 3: Регистрация в фабрике

```python
class ManufacturerParserFactory:
    @staticmethod
    def get_parser(manufacturer_slug: str):
        parsers = {
            'aparici': ApariciParser,
            'dune': DuneParser,
            'my-manufacturer': MyManufacturerParser,  # Добавить здесь
        }
        return parsers.get(manufacturer_slug)
```

### Шаг 4: Тестирование

```python
# test_my_manufacturer.py
from app.services.manufacturer_parsers import MyManufacturerParser

parser = MyManufacturerParser()
collections = parser.extract_collections()

print(f"Найдено {len(collections)} коллекций")
for c in collections[:3]:
    print(f"- {c['title']}: {c['image_url']}")
```

## Устранение проблем

### Проблема: Коллекции без изображений

**Причина**: Неправильные CSS селекторы для данного сайта

**Решение**:
1. Анализировать HTML структуру: `python analyze_aparici_html.py`
2. Найти правильные селекторы
3. Обновить метод `extract_collection_detail()`

### Проблема: Таймауты при скачивании

**Причина**: Медленный сервер или сетевые проблемы

**Решение**:
- Система автоматически повторяет 3 раза
- Между запросами задержка 0.5 сек
- Увеличить timeout в `download_image()` если нужно

### Проблема: Показываются пустые карточки

**Решение**: Теперь исправлено! Автоматическая фильтрация:
- В SQL запросе: `.filter(image_url.isnot(None))`
- При синхронизации: валидация перед сохранением
- Статистика: показывает сколько пропущено

## Настройки производительности

```python
# В extract_collections()
for idx, link in enumerate(collection_links[:20], 1):  # Лимит 20
    # ...
    time.sleep(0.5)  # Задержка 500ms между запросами

# В download_image()
def download_image(image_url, retry_count=3):  # 3 попытки
    response = requests.get(url, timeout=20)  # Таймаут 20 сек
```

## Статистика синхронизации

После каждой синхронизации выводится:

```
✅ Синхронизация завершена:
   ✓ Добавлено: 7 элементов
   ⚠️  Пропущено: 5 элементов (без изображений или названия)
```

## Безопасность и этика

- ✅ User-Agent: идентифицируем себя как браузер
- ✅ Задержки: 500ms между запросами
- ✅ Retry: не более 3 попыток
- ✅ Timeout: 20 секунд максимум
- ✅ Локальное хранение: изображения кэшируются
- ✅ Дедупликация: не скачиваем повторно

## Мониторинг

Все операции логируются в консоль:

```
🔍 Парсинг коллекций Aparici...
  📄 Загрузка: https://...
  Найдено 7 ссылок на коллекции
  🔗 Обработка коллекции 1/7: Ibiza
  ⬇️  Скачивание: https://...
  ✓ Сохранено: aparici_50ee18b2d3.jpg (245876 bytes)
  ✓ Коллекция Ibiza добавлена
```

## FAQ

**Q: Почему некоторые коллекции не появляются?**
A: Система автоматически скрывает коллекции без изображений. Проверьте логи синхронизации.

**Q: Можно ли синхронизировать все сразу?**
A: Да, но лучше по одному для контроля качества.

**Q: Как часто синхронизировать?**
A: Рекомендуется раз в неделю для новых коллекций.

**Q: Где хранятся изображения?**
A: `app/static/uploads/manufacturers/` - автоматически создается.

**Q: Что делать если парсер не работает?**
A: 1) Проверить структуру сайта, 2) Обновить CSS селекторы, 3) Протестировать отдельно.
