# Система автоматического наполнения контентом от производителей

## 📋 Обзор

Реализована система автоматического извлечения и публикации контента с сайтов производителей керамической плитки. Система включает:

- ✅ Управление производителями через админ-панель
- ✅ Автоматический парсинг коллекций, проектов и новостей
- ✅ Публичные страницы для каждого производителя
- ✅ Категоризация контента в блоге
- ✅ Ссылка "Hersteller" в навигации

## 🚀 Установка и запуск

### 1. Установите зависимости

```powershell
pip install beautifulsoup4==4.12.2
```

Или обновите все зависимости:

```powershell
pip install -r requirements.txt
```

### 2. Выполните миграцию базы данных

```powershell
python migrate_manufacturers.py
```

Это создаст новые таблицы:
- `manufacturers` - информация о производителях
- `manufacturer_content` - контент производителей
- Обновит таблицу `blog_posts` новыми полями

### 3. Запустите приложение

```powershell
python run.py
```

## 🎯 Использование

### Добавление производителя

1. Войдите в админ-панель: `http://localhost:5000/admin`
2. Перейдите в раздел **Hersteller** (слева в меню)
3. Заполните форму:
   - **Name**: Название производителя
   - **Slug**: Идентификатор для URL (например: `aparici`, `dune`)
   - **Logo**: Загрузите логотип
   - **Website**: Полный URL сайта производителя
   - **Country**: Страна производителя
   - **Description**: Краткое описание
   - **Active**: Отображать на сайте
   - **Auto Sync**: Автоматическая синхронизация (пока не реализовано)

### Поддерживаемые производители

Система автоматически распознает структуру следующих сайтов:

| Slug | Название | Страна | Website |
|------|----------|--------|---------|
| `aparici` | Aparici | Испания | https://www.aparici.com/de |
| `ape` | APE Grupo | Испания | https://www.apegrupo.com/de |
| `lafabbrica` | La Fabbrica / AVA | Италия | https://www.lafabbrica.it/de |
| `dune` | Dune Ceramics | Испания | https://duneceramics.com/de |
| `equipe` | Equipe Ceramicas | Испания | https://www.equipeceramicas.com/de |
| `casalgrande` | Casalgrande Padana | Италия | https://www.casalgrandepadana.de |

**Важно:** Используйте `slug` точно как указано выше для работы автоматической синхронизации.

### Синхронизация контента

1. В списке производителей нажмите кнопку **🔄** (Synchronisieren)
2. Система автоматически:
   - Извлечет коллекции с сайта производителя
   - Извлечет проекты/реализации
   - Извлечет статьи из блога/новостей
   - Сохранит все в базу данных

### Просмотр контента

**Публичные страницы:**
- Список производителей: `http://localhost:5000/hersteller`
- Страница производителя: `http://localhost:5000/hersteller/{slug}`
- Все коллекции: `http://localhost:5000/hersteller/{slug}/collection`
- Все проекты: `http://localhost:5000/hersteller/{slug}/project`
- Все новости: `http://localhost:5000/hersteller/{slug}/blog`

## 📁 Структура файлов

```
app/
├── models.py                              # Новые модели: Manufacturer, ManufacturerContent
├── routes.py                              # Роуты для производителей
├── services/
│   └── content_scraper_service.py         # Сервис парсинга контента
└── templates/
    ├── admin/
    │   ├── base_admin.html               # Обновлено: добавлена ссылка Hersteller
    │   └── manufacturers.html            # Админка управления производителями
    └── public/
        ├── manufacturers.html             # Список всех производителей
        ├── manufacturer_detail.html       # Детальная страница производителя
        └── manufacturer_content_list.html # Список контента производителя
```

## 🔧 Модели базы данных

### Manufacturer

```python
id              INTEGER PRIMARY KEY
name            VARCHAR(255)      # Название
slug            VARCHAR(255)      # URL-идентификатор
logo            VARCHAR(255)      # Файл логотипа
website         VARCHAR(500)      # Сайт производителя
description     TEXT              # Описание
country         VARCHAR(100)      # Страна
active          BOOLEAN           # Активен
auto_sync       BOOLEAN           # Автосинхронизация
last_sync       DATETIME          # Последняя синхронизация
order           INTEGER           # Порядок сортировки
```

### ManufacturerContent

```python
id              INTEGER PRIMARY KEY
manufacturer_id INTEGER           # FK на Manufacturer
content_type    VARCHAR(50)       # collection/project/blog/news
title           VARCHAR(255)      # Заголовок
subtitle        VARCHAR(255)      # Подзаголовок
description     TEXT              # Краткое описание
full_content    TEXT              # Полный контент
image_url       VARCHAR(500)      # URL изображения
source_url      VARCHAR(500)      # Ссылка на оригинал
technical_specs TEXT              # JSON с характеристиками
published       BOOLEAN           # Опубликовано
featured        BOOLEAN           # Избранное
order           INTEGER           # Порядок
views           INTEGER           # Просмотры
```

### BlogPost (обновлено)

Добавлены новые поля:
```python
category        VARCHAR(100)      # Категория (Neue Kollektionen, Projekte, etc.)
manufacturer_id INTEGER           # FK на Manufacturer (опционально)
image_url       VARCHAR(500)      # URL изображения
published       BOOLEAN           # Опубликовано
```

## 🎨 Типы контента

### Collections (Коллекции)
- Названия коллекций плитки
- Изображения образцов
- Краткие описания
- Ссылки на детальные страницы производителя

### Projects (Проекты)
- Реализованные проекты с использованием плитки
- Фотографии интерьеров
- Описания применения

### Blog/News (Новости)
- Статьи из блогов производителей
- Анонсы новых коллекций
- Участие в выставках (Cersaie, EuroShop)
- Дизайнерские тренды

## 📝 Категории для блога

Рекомендуемые категории для BlogPost:
- **Neue Kollektionen** - Новые коллекции
- **Projekte** - Проекты и реализации
- **Design-Trends** - Тренды дизайна
- **Ratgeber** - Руководства и советы
- **Unternehmensnews** - Новости компаний

## 🔄 Алгоритм парсинга

Система использует BeautifulSoup для извлечения контента:

1. **Извлечение коллекций:**
   - Поиск ссылок на страницы коллекций
   - Извлечение названий, описаний, изображений
   - Ограничение: до 20 коллекций за раз

2. **Извлечение проектов:**
   - Поиск карточек проектов
   - Извлечение заголовков, описаний, фото
   - Ограничение: до 15 проектов за раз

3. **Извлечение статей:**
   - Поиск постов в блоге/новостях
   - Извлечение заголовков, анонсов, дат
   - Ограничение: до 10 статей за раз

## ⚠️ Известные ограничения

1. **Парсинг не идеален** - разные сайты имеют разную структуру, некоторый контент может быть пропущен
2. **Изображения** - URL изображений могут быть относительными или требовать авторизации
3. **Обновление контента** - пока нет автоматического обновления, только ручная синхронизация
4. **Перевод** - контент извлекается в оригинальном языке (обычно немецкий/английский)

## 🚀 Расширение функционала

### Добавление нового производителя

Отредактируйте `app/services/content_scraper_service.py`:

```python
self.manufacturer_configs = {
    # ... существующие ...
    'новый_slug': {
        'base_url': 'https://www.новыйпроизводитель.com/de',
        'collections_path': '/kollektionen',
        'projects_path': '/projekte',
        'blog_path': '/blog'
    }
}
```

### Настройка селекторов

Если структура сайта отличается, можно доработать методы:
- `extract_collections()`
- `extract_projects()`
- `extract_blog_posts()`

Добавить специфичные CSS-селекторы или XPath для конкретного производителя.

## 📊 Мониторинг и логи

- Все ошибки парсинга выводятся в консоль
- Время последней синхронизации сохраняется в `manufacturer.last_sync`
- Количество извлеченных элементов отображается в админке

## 🎯 Дальнейшие улучшения

- [ ] Автоматическая синхронизация по расписанию (CRON)
- [ ] Кэширование изображений локально
- [ ] Автоматический перевод контента
- [ ] Дедупликация контента
- [ ] API для программного доступа
- [ ] Статистика популярности контента
- [ ] Push-уведомления о новом контенте

## 🆘 Решение проблем

### Ошибка "Manufacturer not found"
- Убедитесь, что slug производителя соответствует списку поддерживаемых
- Проверьте, что производитель активен (`active=True`)

### Контент не загружается
- Проверьте URL сайта производителя
- Убедитесь, что сайт доступен
- Проверьте логи в консоли на наличие ошибок парсинга

### Нет изображений
- Некоторые сайты используют ленивую загрузку (lazy loading)
- URL изображений могут быть защищены от hotlinking
- Рассмотрите локальное кэширование изображений

---

**Автор:** AI Assistant  
**Дата:** 12.02.2026  
**Версия:** 1.0
