# Улучшения парсеров - AI Content Processing

## Что изменилось

### ✅ Добавлена AI обработка контента через OpenAI

**Новый файл:** `app/services/content_processor.py`
- Обработка описаний коллекций через GPT-4o-mini
- Обработка описаний проектов
- Автоматическая адаптация текста для немецкого рынка (Frankfurt)
- Удаление лишних маркетинговых фраз
- Добавление локальных призывов к действию (CTA)

**Обновлен:** `app/services/manufacturer_parsers.py`
- Добавлен метод `process_content_with_ai()` в `BaseManufacturerParser`
- Все парсеры теперь автоматически получают AI обработку
- Обновлен `ApariciParser.extract_collection_detail()` - использует AI
- Обновлен `ApariciParser.extract_projects()` - использует AI

## Как это работает

### До улучшений:
```
Сайт производителя → Парсер → Сырой текст → БД
```
Проблемы:
- Текст на разных языках (испанский, английский)
- Маркетинговые клише ("Discover our amazing...")
- Нет локальной адаптации
- Нет призыва к действию

### После улучшений:
```
Сайт производителя → Парсер → Сырой текст → OpenAI → Обработанный текст → БД
```
Преимущества:
- ✅ Весь текст на немецком языке
- ✅ Удалены маркетинговые клише
- ✅ Адаптировано для Frankfurt/Rhein-Main
- ✅ Локальный CTA (адрес showroom, парковка)
- ✅ SEO-оптимизированные описания

## Структура обработки

### Для коллекций (`process_collection_description`):

**Вход:**
- Raw text с сайта производителя
- Название коллекции
- Название производителя

**Выход:**
- `description` (150-200 символов) - краткое описание для карточек
- `full_content` (200-300 слов, HTML) - полное описание для страницы коллекции

**Промпт для OpenAI:**
- Профессиональный, но доступный тон
- Фокус на практические преимущества
- Описание стиля, материалов, областей применения
- CTA с адресом showroom в Frankfurt

### Для проектов (`process_project_description`):

**Вход:**
- Raw text с сайта
- Название проекта
- Производитель

**Выход:**
- `description` (max 150 символов)
- `full_content` (100-150 слов, HTML)

**Промпт:**
- Фокус на дизайн-вдохновение
- Тип проекта (отель, ресторан, дом)
- Использованные коллекции
- Вдохновение для собственных проектов

## AI Prompt Strategy

### System Prompt:
```
Du bist ein Experte für Fliesenmarketing in Deutschland mit Fokus auf Frankfurt.

WICHTIG:
- Schreibe auf Deutsch (für deutsches Publikum)
- Ton: Professionell, aber zugänglich und einladend
- Entferne Marketing-Floskeln
- Fokus auf praktische Vorteile
- Zielgruppe: Endkunden (Privat + Architekten)
```

### User Prompt включает:
- Название коллекции/проекта
- Производитель
- Сырой текст (до 1500 символов)
- Инструкции по формату выхода

### Локальный CTA (всегда добавляется):
```
Diese Kollektion ist in unserem Showroom in Frankfurt 
(Hanauer Landstraße 421) ausgestellt. Besuchen Sie uns 
für eine persönliche Beratung – kostenlose Parkplätze 
direkt vor Ort.
```

## Fallback механизм

Если OpenAI API недоступен или ошибка:
- Генерируется базовое описание
- Fallback на немецком языке
- Включает основную информацию (название, производитель)
- Добавляется минимальный CTA

## Использование

### Автоматически (для всех парсеров):

```python
# В любом парсере наследнике BaseManufacturerParser:
processed = self.process_content_with_ai(
    raw_text="Original text from manufacturer site...",
    title="Collection Name",
    content_type='collection'  # или 'project'
)

description = processed['description']
full_content = processed['full_content']  # HTML formatted
```

### Пример для нового парсера:

```python
class MyManufacturerParser(BaseManufacturerParser):
    def extract_collection_detail(self, url: str) -> Dict:
        soup = self.fetch_page(url)
        
        # 1. Извлекаем сырой текст
        raw_text = soup.find('div', class_='description').get_text()
        title = soup.find('h1').get_text()
        
        # 2. Обрабатываем через AI
        processed = self.process_content_with_ai(
            raw_text=raw_text,
            title=title,
            content_type='collection'
        )
        
        # 3. Возвращаем обработанные данные
        return {
            'description': processed['description'],
            'full_content': processed['full_content'],
            # ... другие поля
        }
```

## Производительность

### Время обработки:
- Одна коллекция: ~2-4 секунды (OpenAI API call)
- 20 коллекций: ~1-2 минуты (с задержками между запросами)

### Рекомендации:
- Используйте задержки между запросами к OpenAI
- Кешируйте результаты (description/full_content не меняются часто)
- Рассмотрите батч-обработку для больших объемов

## Стоимость OpenAI

**Модель:** gpt-4o-mini
**Цена (февраль 2026):**
- Input: $0.15 / 1M tokens
- Output: $0.60 / 1M tokens

**Примерная стоимость:**
- 1 описание коллекции: ~500 tokens input + ~300 tokens output = ~$0.0003
- 100 коллекций: ~$0.03
- 1000 коллекций: ~$0.30

Очень доступно! ✅

## Тестирование

Создан тестовый скрипт: `test_content_processor.py`

```bash
# Тестирование AI обработки
python test_content_processor.py
```

## Mobile Responsiveness

### Что уже есть ✅:
- Viewport meta tag
- Bootstrap 5 адаптивная сетка
- Media queries для 768px и 576px
- Адаптация hero секции, карусели, чата

### Что можно улучшить:
- Добавить брейкпоинты для 992px, 1200px
- Оптимизировать кнопки на мобильных
- WebP изображения с srcset

## Следующие шаги

### Приоритет 1:
- [ ] Протестировать на других парсерах (APE, Baldocer, Exagres...)
- [ ] Добавить кеширование обработанного контента
- [ ] Реализовать батч-обработку для больших обновлений

### Приоритет 2:
- [ ] Добавить обработку для blog posts
- [ ] Многоязычная поддержка (английский для B2B)
- [ ] A/B тестирование разных промптов

### Приоритет 3:
- [ ] Автоматическое извлечение технических характеристик через AI
- [ ] Генерация SEO-оптимизированных meta descriptions
- [ ] Автоматическая категоризация (стиль, применение, материал)

## Логирование

AI обработка логируется в консоль:
```
🔗 Обработка коллекции 1/20: Collection Name
  🤖 AI обработка описания для Collection Name...
  ✓ Коллекция Collection Name добавлена
```

## Заключение

Эта система значительно улучшает качество контента на сайте:
- Профессиональные немецкие описания вместо сырого текста с сайтов
- Локальная адаптация для Frankfurt рынка
- SEO-оптимизация
- Единообразие стиля
- Экономия времени (не нужно вручную переписывать тексты)

**Результат:** Более профессиональный сайт, лучше SEO, выше конверсия! 🚀
